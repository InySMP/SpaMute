# 
# 💎 SpaMute - 設定檔 💎
# 

# 請勿觸碰此物 #
version: ${version}

# --- 刷屏檢測設定 ---
spam-detection:
  # 玩家在 'time-window-seconds' 內發送 'max-messages' 條消息將被視為刷屏。
  # 您的要求是 4 次刷屏 => 禁言 1 天 (即 4 次懲罰觸發)
  
  # 偵測窗口（秒）：在此時間內累計的消息數量
  time-window-seconds: 3 
  
  # 最大消息數：在偵測窗口內允許發送的最大消息數
  max-messages: 5 
  
  # 懲罰閾值：玩家需要觸發多少次刷屏（即超過 max-messages）才會被禁言
  mute-threshold: 4
  
# --- 禁言設定 ---
mute:
  # 禁言時長 (秒)。 1 天 = 86400 秒
  duration-seconds: 86400 
  
  # 禁言結束後是否自動解除禁言
  auto-unmute: true 
  
  # 禁言懲罰觸發後，是否重置刷屏計數
  reset-count-on-mute: true